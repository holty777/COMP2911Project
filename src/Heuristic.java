
public class Heuristic implements Strategy {
	
	public int getH(Maze state){
		
		//get list of required trips
		//calculate the cost from current location to start location of the required trip
		//add the end point of the travel time with the wait time to cost 
		
		//comapre all the cost (maybe use priority queue) 
		//and return the lowest cost from x to b 
		return 0;
	}
}
